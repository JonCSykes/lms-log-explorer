# Docker Compose configuration
# Copy to .env and edit values for your machine.

# Host path to LM Studio server logs.
# Example (macOS): /Users/<you>/.lmstudio/server-logs
LMS_LOG_ROOT_HOST=./lms-logs

# Exposed port for the web UI.
LMS_LOG_EXPLORER_PORT=3000

# Node heap size in MB for container runtime.
# Increase if you have very large indexed datasets.
NODE_MAX_OLD_SPACE_SIZE=4096
